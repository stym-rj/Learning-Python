import random       #random is a module which is being imported using 'import' keyword.
#'random' module generates random numbers or selects random choices.

print(random.randint(1, 30))            #generating random numbers from 1 to 30 using the 'randint' function within the 'random' module.