for x in 0,1,2,5,6:       #if we do this instead of putting the range() function, output will be same as the written sequence.
    print(x)

print("another sequence: ")

for x in 1,1,5,7,4:       #sequence can be random also. #suppose we have list of numbers to be added, then we can use this and in code-block, we can write as "sum=sum+x' after initializing sum to 0 outside the for loop
    print(x)